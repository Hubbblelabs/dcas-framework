MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/dcas?retryWrites=true&w=majority

# Generate a strong secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-strong-secret-here

# LOCAL DEVELOPMENT
NEXTAUTH_URL=http://localhost:3000

# PRODUCTION — set this in your hosting environment (Vercel / Hostinger / etc.)
# NEXTAUTH_URL=https://dcas.teammistake.com

# SMTP Configuration
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-email-password

# Optional: Customize the "From" address
SMTP_FROM="DCAS Assessment" <your-email@yourdomain.com>

# Translation API Configuration (for multilingual question support)
# Set these to enable automatic translation of questions to Indian languages
# Example: Google Translate API, Azure Translator, DeepL, etc.
TRANSLATION_API_URL=https://translate.googleapis.com
TRANSLATION_API_KEY=your-translation-api-key-here